# Commands

`true-up build` builds the repository's dependency graph. This sentence derives from the `build`
entry in `data/commands.json`, so changing the `status` entry alone does not make this page stale.
